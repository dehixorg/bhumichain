version: '3.8'

networks:
  bhumichain:
    name: bhumichain_network

volumes:
  peer1.revenuedept.bhumichain.in:
  couchdb1:
  ipfs_data:
  ipfs_staging:

services:
  # ── CouchDB for peer1 ────────────────────────────────────────────────────────
  couchdb1:
    image: couchdb:3.3
    restart: unless-stopped
    environment:
      COUCHDB_USER: admin
      COUCHDB_PASSWORD: adminpw
    ports:
      - "7984:5984"
    volumes:
      - couchdb1:/opt/couchdb/data
    networks:
      - bhumichain

  # ── Peer 1 ───────────────────────────────────────────────────────────────────
  peer1.revenuedept.bhumichain.in:
    image: hyperledger/fabric-peer:2.5
    restart: unless-stopped
    environment:
      CORE_VM_ENDPOINT: unix:///host/var/run/docker.sock
      CORE_VM_DOCKER_HOSTCONFIG_NETWORKMODE: bhumichain_network
      FABRIC_LOGGING_SPEC: INFO
      CORE_PEER_TLS_ENABLED: "true"
      CORE_PEER_PROFILE_ENABLED: "false"
      CORE_PEER_TLS_CERT_FILE: /etc/hyperledger/fabric/tls/server.crt
      CORE_PEER_TLS_KEY_FILE: /etc/hyperledger/fabric/tls/server.key
      CORE_PEER_TLS_ROOTCERT_FILE: /etc/hyperledger/fabric/tls/ca.crt
      CORE_PEER_ID: peer1.revenuedept.bhumichain.in
      CORE_PEER_ADDRESS: peer1.revenuedept.bhumichain.in:9051
      CORE_PEER_LISTENADDRESS: 0.0.0.0:9051
      CORE_PEER_CHAINCODEADDRESS: peer1.revenuedept.bhumichain.in:9052
      CORE_PEER_CHAINCODELISTENADDRESS: 0.0.0.0:9052
      CORE_PEER_GOSSIP_EXTERNALENDPOINT: 20.198.127.97:9051
      # Connect back to Peer 0 on VM1
      CORE_PEER_GOSSIP_BOOTSTRAP: 4.213.3.32:7051
      CORE_PEER_LOCALMSPID: RevenueDeptMSP
      CORE_PEER_MSPCONFIGPATH: /etc/hyperledger/fabric/msp
      CORE_OPERATIONS_LISTENADDRESS: 0.0.0.0:17052
      CORE_METRICS_PROVIDER: prometheus
      CORE_LEDGER_STATE_STATEDATABASE: CouchDB
      CORE_LEDGER_STATE_COUCHDBCONFIG_COUCHDBADDRESS: couchdb1:5984
      CORE_LEDGER_STATE_COUCHDBCONFIG_USERNAME: admin
      CORE_LEDGER_STATE_COUCHDBCONFIG_PASSWORD: adminpw
    volumes:
      - /var/run/docker.sock:/host/var/run/docker.sock
      - ./crypto-config/peerOrganizations/revenuedept.bhumichain.in/peers/peer1.revenuedept.bhumichain.in/msp:/etc/hyperledger/fabric/msp
      - ./crypto-config/peerOrganizations/revenuedept.bhumichain.in/peers/peer1.revenuedept.bhumichain.in/tls:/etc/hyperledger/fabric/tls
      - peer1.revenuedept.bhumichain.in:/var/hyperledger/production
    working_dir: /root
    command: peer node start
    ports:
      - "9051:9051"
      - "17052:17052"
    depends_on:
      - couchdb1
    # NOTE: You will need to add extra_hosts if DNS isn't resolving properly between VMs
    extra_hosts:
      - "orderer.bhumichain.in:4.213.3.32"
      - "peer0.revenuedept.bhumichain.in:4.213.3.32"
    networks:
      - bhumichain

  # ── IPFS Storage Node ────────────────────────────────────────────────────────
  ipfs:
    image: ipfs/kubo:latest
    restart: unless-stopped
    ports:
      - "4001:4001"
      - "4001:4001/udp"
      - "127.0.0.1:8080:8080" # IPFS Gateway
      - "127.0.0.1:5001:5001" # IPFS API
    volumes:
      - ipfs_staging:/export
      - ipfs_data:/data/ipfs
    networks:
      - bhumichain
